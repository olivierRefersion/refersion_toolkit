const axios = require("axios");

const uploadorders = (req, res) => {
    res.json({ status: 'success', message: 'Just testing for now'})
}
//TODO gotta add all the logic here as well.
module.exports.uploadorders = uploadorders;