import { Alert } from "../components"

export default function BulkManualOrderCredits() {
    return (
        <main>
            <div className="container">
                <Alert type='info'>Something related to bulk manual order credits</Alert>
            </div>
        </main>
    
    )
}